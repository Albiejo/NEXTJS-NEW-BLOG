{
  "extends": "next/core-web-vitals",
  "rules": {
    "@next/next/no-img-element": "off", // Disable the rule for using <img> instead of <Image>
    "react/no-unescaped-entities": "off" // Disable the rule for unescaped entities like '
  }
}
